# Make gvim the default editor
export EDITOR="vim "

export PATH=$PATH:~/bin
# Don’t clear the screen after quitting a manual page
# export MANPAGER="less -X"

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups
# Make some commands not show up in history
export HISTIGNORE="ls:ls *:cd:cd -:pwd;exit:date:* --help"
 
# Jekyll environment variables for source and destination
export JEKYLL_SOURCE="/home/prasanna/Dropbox/jekyllblog"
export JEKYLL_DESTINATION="/home/prasanna/Dropbox/wordpras.github.io"
